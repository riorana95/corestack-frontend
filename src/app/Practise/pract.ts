import { Component } from "@angular/core";

@Component({
    template: ""
})
export class Pract{
    // Redcude method 3 question
//     const employees = [
//   { name: 'Alice', department: 'Engineering', salary: 70000 },
//   { name: 'Bob', department: 'HR', salary: 50000 },
//   { name: 'Charlie', department: 'Engineering', salary: 80000 },
//   { name: 'David', department: 'HR', salary: 55000 }
// ];


// const groupedEmployees = employees.reduce((acc, employee) =>{
//     const dept  = employee.department;
//     if(!acc[dept]){ acc[dept] = [];}
//     acc[dept].push(employee);
//     return acc;
// },{})

// console.log(groupedEmployees);

// ----------------------

// const techStack = ['Angular', 'Java', 'Angular', 'SQL', 'Java', 'Angular'];

// const counter = techStack.reduce((acc,tech)=>{
//         if(!acc[tech]){
//             acc[tech] = 1
//         }else{
//             acc[tech] += 1
//         }
//         return acc; 
// },{})

// console.log(counter)

// ----------------------
// const numericGroups = [[10, 20], [30, 40], [50]];

// const total = numericGroups.reduce((acc,current)=>{
//    let sum = 0
//    const listsum = current.map(x=>{
//        sum +=x
//        return sum;
//    })
//    return acc+=sum
// },0)

// console.log(total)

// ----------------------
    // search input
    // pagination
    // custom pipe
    // custom directive stuctural
    // custom directive Attribute
    // async/sync/promise/behavoiur subject/Promise chaining
    // features of angular 11|16|19|21
} 
